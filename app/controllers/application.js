import Controller from '@ember/controller';

// Uncomment
// import dedent from 'dedent';

export default Controller.extend({

  text: `
    # Hello, world!
  `

  // Uncomment after starting server to see error. Restart server to see it working.
  // text: dedent`
  //   # Hello, world!
  // `

});
